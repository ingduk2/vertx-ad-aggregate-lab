spring:
  application:
    name: vertx-ad-aggregate-lab
  datasource:
    url: jdbc:mariadb://localhost:3306/ad_aggregate
    username: ad
    password: ad
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
